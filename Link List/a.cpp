#include <ctime>
#include <iostream>
#include <random>

using namespace std;

int main()
{
    // Create a random number generator and a uniform distribution
    // Initialize random number generator using hardware entropy

    // Generate 5 random numbers
    for (int i = 0; i < 10; ++i)
    {
        cout < < < < endl; // Generate and print random number
    }

    return 0;
}
